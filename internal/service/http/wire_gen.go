// Code generated by Wire. DO NOT EDIT.

//go:generate wire
//+build !wireinject

package http

import (
	"ratelimit/internal/config"
	"ratelimit/internal/delivery/http"
	"ratelimit/internal/wireset"
)

// Injectors from wire.go:

func Initialize(configPath string) (Application, error) {
	configConfig, err := config.NewConfig(configPath)
	if err != nil {
		return Application{}, err
	}
	logger, err := wireset.InitLogger(configConfig)
	if err != nil {
		return Application{}, err
	}
	ratelimiter, err := wireset.InitRateLimiter(configConfig)
	if err != nil {
		return Application{}, err
	}
	httpServer := http.NewHttpServer(ratelimiter)
	application := newApplication(logger, configConfig, httpServer)
	return application, nil
}
